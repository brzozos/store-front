export interface Product {
    id: string;
    name: string;
    unitPrice: number;
    amount: number;
}

export interface ProductDTO {
    id: string;
    name: string;
    unitPrice: number;
    amount: number;
}
